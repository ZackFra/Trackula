spring:
  application:
    name: track
  h2:
    console:
      enabled: true
  sql:
    init:
      mode: always
  security:
    filter:
      order: 10
logging:
  level:
    root: INFO
server:
  error:
    include-message: always
    include-binding-errors: always